/** @format */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import SafeViewApp from "./SafeViewApp";

AppRegistry.registerComponent(appName, () => SafeViewApp);
